
PREFIX=
OGRE_PATH=$(PREFIX)/usr/local
OIS_PATH=$(PREFIX)/usr/local

CFLAGS=-DOGRE_GUI_GLX -DOGRE_CONFIG_LITTLE_ENDIAN -I${ORGE_PATH}/include -I${OGRE_PATH}/include/OGRE -I${OIS_PATH}/include/OIS -I${OIS_PATH}/include
LIBS=-L${OGRE_PATH}/lib -lOgreMain -L${OIS_PATH}/lib -lOIS
